import { MiembrosInteresados } from './miembros-interesados';

describe('MiembrosInteresados', () => {
  it('should create an instance', () => {
    expect(new MiembrosInteresados()).toBeTruthy();
  });
});
