import { PosicionGeografica } from './posicion-geografica';

describe('PosicionGeografica', () => {
  it('should create an instance', () => {
    expect(new PosicionGeografica()).toBeTruthy();
  });
});
