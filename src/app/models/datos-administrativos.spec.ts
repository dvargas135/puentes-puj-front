import { DatosAdministrativos } from './datos-administrativos';

describe('DatosAdministrativos', () => {
  it('should create an instance', () => {
    expect(new DatosAdministrativos()).toBeTruthy();
  });
});
